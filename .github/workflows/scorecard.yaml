name: Scorecards supply-chain security
on:
  # Only the default branch is supported.
  branch_protection_rule:
  schedule:
    - cron: '30 1 * * 6'
  push:
    branches: ["main"]

# Declare default permissions as read only.
permissions: read-all

jobs:
  run:
    uses: defenseunicorns/uds-common/.github/workflows/callable-scorecard.yaml@879295b9714bc7fa5fe7db7b4de2c245f1b406e7 # v1.0.0
