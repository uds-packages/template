{{- if .Values.adminIngress.ipAddressPool }}
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: admin-ingressgateway
  namespace: metallb-system
spec:
  addresses:
    {{- if not (empty .Values.adminIngress.ipAddressPool) }}
    {{- if (kindIs "string" .Values.adminIngress.ipAddressPool) }}
    - {{ .Values.adminIngress.ipAddressPool }}/32
    {{- else }}
    {{- range .Values.adminIngress.ipAddressPool }}
    - {{ . }}/32
    {{- end }}
    {{- end }}
    {{- end }}
  serviceAllocation:
    priority: 50
    namespaces:
      - {{ .Values.adminIngress.namespace }}
    serviceSelectors:
      - matchLabels:
          {{ .Values.adminIngress.matchLabel }}
{{- end }}
